#include <stdio.h>

int pre[1001], in[1001], inFrom[1001];
int rootIdx;

int main() {
  int t;
  scanf("%d", &t);

  // 반복횟수
  while (t--) {
    int n;
    // node 길이
    scanf("%d", &n);
    for (int i = 1; i <= n; i++) scanf("%d", pre + i)
  }

  return 0;
}
